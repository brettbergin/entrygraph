; Method calls and object creations. The shaper reads the `function` /
; `type` fields to derive callee text, receiver, and constructed-type name.
(invocation_expression) @call
(object_creation_expression) @new
