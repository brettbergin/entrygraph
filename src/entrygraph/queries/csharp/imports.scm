; C# `using` directives: plain (`using System.Diagnostics;`), aliased
; (`using Alias = System.Collections.Generic;`), and static
; (`using static System.Math;`). The shaper distinguishes them.
(using_directive) @import
