; Harvest-level queries: find SDL definition nodes; the GraphQL shaper walks
; each captured node for its name, fields, and descriptions.

(object_type_definition) @def.object_type

(object_type_extension) @def.object_type_ext

(interface_type_definition) @def.interface

(input_object_type_definition) @def.input

(enum_type_definition) @def.enum

(union_type_definition) @def.union

(root_operation_type_definition) @def.root_op
